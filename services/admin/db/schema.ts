import { pgTable } from "drizzle-orm/pg-core";

export const admins = pgTable("admins", {});

export const clinics = pgTable("clinics", {});

export const keys = pgTable("keys", {});

export const fourElements = pgTable("fourElements", {});

export const circleOfLife = pgTable("circleOfLife", {});

export const selfKnowledge = pgTable("selfKnowledge", {});

export const loveLanguage = pgTable("loveLanguage", {});
